#[test_only]
module nft_protocol::test_ob_witness_protected_actions {
    // TODO: assert that OB can be configured to only enable certain actions
    // with witness protection (see the `WitnessProtectedActions` struct)
    //
    // assert that these actions can be granulary configured
}
