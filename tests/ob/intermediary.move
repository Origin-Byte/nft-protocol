#[test_only]
module nft_protocol::test_ob_intermediary {
    // TODO:
    // - create `TradeIntermediary` by matching bid and `finish_trade` (test assertion)
    // - create `TradeIntermediary` by matching ask and `finish_trade` (test assertion)
}
